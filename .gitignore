# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Secrets — NEVER commit
.env

# Logs
*.log
service.log
smoke.log
smoke.err

# Watcher state — must NOT be committed (per-deployment cursor)
state.json

# Brand-docs extraction cache (rebuilt per host on first run)
docs/.cache.json

# OS junk
Thumbs.db
.DS_Store
